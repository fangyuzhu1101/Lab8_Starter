// unit.test.js

const functions = require('../code-to-unit-test/unit-test-me.js');

// TODO - Part 2
/*
test('if (346)702-2222 is a valid phone number', () => {
    expect(unitTest.isPhoneNumber("(346)702-2222")).toBe(true);
});
test('if 222-111-5599 is a valid phone number', () => {
    expect(unitTest.isPhoneNumber("222-111-5599")).toBe(true);
});

test('if 878-99-3333 is a valid phone number', () => {
    expect(unitTest.isPhoneNumber("878-99-3333")).toBe(false);
});
test('if (291)111-xyzk is a valid phone number', () => {
    expect(unitTest.isPhoneNumber("(291)111-xyzk")).toBe(false);
});
*/
